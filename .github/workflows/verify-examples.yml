# https://help.github.com/en/articles/workflow-syntax-for-github-actions

on: push
name: Examples check
jobs:
  examples-cjs:
    name: Build and run examples (CommonJS)
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
        - windows-latest
        - macos-latest
        - ubuntu-latest
        nodejs-build-example:
        - "6.9.0" # minimum version Babel CLI works
        nodejs-run-example:
        - "4.0.0" # target version
        example:
        - "babel-cjs"
        - "ts-babel-cjs"
        - "ts-tsc-cjs"
        exclude:
        - os: windows-latest # not work "babel --extensions '.ts'" on Windows?
          example: "ts-babel-cjs"
      fail-fast: false
    steps:
    - name: Checkout source codes
      uses: actions/checkout@v3

    - name: Build
      uses: ./.github/actions/build
    - name: Clear modules
      run: npm run clear-modules

    - name: Install Node.js (build example)
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.nodejs-build-example }}
    - name: Output versions
      run: |
        node -v
        npm -v
    - name: Install dependencies (example)
      run: yarn
      working-directory: ./examples/${{ matrix.example }}
    - name: Build example
      run: npm run build
      working-directory: ./examples/${{ matrix.example }}

    - name: Install Node.js (run example)
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.nodejs-run-example }}
    - name: Output versions
      run: |
        node -v
        npm -v
    - name: Run example
      run: node ./dist/index.js
      working-directory: ./examples/${{ matrix.example }}

  examples-esm:
    name: Build and run examples (ES Modules)
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
        - windows-latest
        - macos-latest
        - ubuntu-latest
        nodejs-build-example:
        - "6.9.0" # minimum version Babel CLI works
        nodejs-run-example:
        - "12.17.0" # minimum version {"type": "module"} supports in package.json
        example:
        - "babel-esm"
        - "ts-babel-esm"
        - "ts-tsc-esm"
        exclude:
        - os: windows-latest # not work "babel --extensions '.ts'" on Windows?
          example: "ts-babel-esm"
      fail-fast: false
    steps:
    - name: Checkout source codes
      uses: actions/checkout@v3

    - name: Build
      uses: ./.github/actions/build
    - name: Clear modules
      run: npm run clear-modules

    - name: Install Node.js (build example)
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.nodejs-build-example }}
    - name: Output versions
      run: |
        node -v
        npm -v
    - name: Install dependencies (example)
      run: yarn
      working-directory: ./examples/${{ matrix.example }}
    - name: Build example
      run: npm run build
      working-directory: ./examples/${{ matrix.example }}

    - name: Install Node.js (run example)
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.nodejs-run-example }}
    - name: Output versions
      run: |
        node -v
        npm -v
    - name: Run example
      run: node ./dist/index.js
      working-directory: ./examples/${{ matrix.example }}
